export enum LOTRResources {
  BOOK = 'book',
  MOVIE = 'movie',
  CHAPTER = 'chapter',
  CHARACTER = 'character',
  QUOTE = 'chapter',
}

export enum SortDirection {
  ASCENDING = 'asc',
  DESCENDING = 'desc',
}

export enum ResponseFields {
  DOCS = 'docs',
  TOTAL = 'total',
  LIMIT = 'limit',
  OFFSET = 'offset',
  PAGE = 'page',
  PAGES = 'pages',
}
